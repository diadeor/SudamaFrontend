import Link from "next/link";
import { ReactNode } from "react";

const MobileLayout = ({ child }: { child: ReactNode }) => {
  return (
    <div className="okay">
      <p>okay</p>
    </div>
  );
};

export default MobileLayout;
